Website landing page screenshot, desktop 16:9, for a Chinese command-line knowledge corpus product named 命令行语料数据库. This is a UI design comp of a real webpage — a split-hero variation of a knowledge-graph landing that also previews the content below the fold.
Structure, in order: (1) Hero split into two columns, 64% height. Left column 58% width: the knowledge graph as a dense constellation panel on deep ink-blue #0d1330 — amber-gold #e8b44a nodes, hairline edges, one focused cluster 大模型训练 expanded with command nodes deepspeed, accelerate, torchrun and small satellite tags bp-deepspeed; a small search input overlaid at its top-left reading 搜索命令…. Right column 42% width, on cream-paper #f5ead0: the corpus anatomy — a heading 什么是每条命令的三件套, then one real command card for deepspeed rendered as an indexed sheet: three stamped sections 内涵外延 / 生产最佳实践 / 风险提示 each with two lines of real Chinese text (多机多卡训练启动器, ZeRO 分片与梯度检查点配置, 显存溢出与通信超时风险), a footer row linking 大模型训练/deepspeed.md 与 bp-deepspeed.md. (2) Below the hero, a full-width horizontal pipeline band on deep ink: YAML → Markdown → CLI as flat amber pictogram steps with small captions 单一数据源 / 双向链接语料 / 检索与校验. (3) Bottom strip: stats row 100 领域 · 1137 命令页 · 741 最佳实践页 and primary action 克隆语料库 amber pill, secondary 命令搜索 ghost button.
Visual world: star-chart cartography meets indexed reference card — deep ink field with amber nodes on the left, cream paper sheet with ink text and small amber stamps on the right, hard hairline rules between columns, no gradients, no neon. All text in Chinese, real content only.
